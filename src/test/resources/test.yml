##### redis的配置信息
redis:
  host: 172.31.223.30
  port: 16379
  password: iflytek!



animation:
  dataType:
    replace:
      String->std::string

#  commonTypeNameMethodInvocation:
#    typeName:
#      Thread
#      identifier:


  testToolMethodName:
    replace:
      assertTrue->ASSERT_TRUE
      assertFalse->ASSERT_FALSE
      assertEquals->ASSERT_EQ
      assertNotEquals->ASSERT_NE
      assertNotSame->ASSERT_NE
      assertSame->ASSERT_EQ


  common:
    replace:


  methodName:
    replace:
      isPaused->IsPending
      start->Play
      isStarted->IsRunning
      isRunning->IsRunning
      pause->Pause

    remove:
      name5


